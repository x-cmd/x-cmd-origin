# shellcheck shell=sh disable=SC2039,SC2142,SC3043

lack_subcmd(){
    param:subcmd lack_subcmd                    \
        repo          "repo command"            \
        current       "set current owner, repo"
    param:subcmd:try
    param:run
}

lack_subcmd_current(){
    param:void
}

test_lack_subcmd(){
    @assert false X_CMD_ADVISE_FUNC_NAME=lack_subcmd lack_subcmd _x_cmd_advise_json
}
@assert:file:func