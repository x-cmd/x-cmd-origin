# shellcheck shell=sh disable=SC1072 #source

test_get_user_info(){
    @assert "$(x hub user)" "=~" "[a-z]+ [a-z0-9]+"
}

test_logout(){
    x hub logout
    @assert "$(x hub user)" "=~" "^$"
}

@assert:file:func

