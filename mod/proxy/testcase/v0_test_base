# shellcheck shell=sh disable=SC3043

rm -rf ~/.x-cmd/proxy
mkdir -p ~/.x-cmd/proxy
cp -r ./_v0/ ~/.x-cmd/proxy/_v0/
cp ./v0 ~/.x-cmd/proxy/v0
cp ./latest ~/.x-cmd/proxy/latest

xrc assert
x log proxy
xrc proxy

if [ -n "${BASH_VERSION}${ZSH_VERSION}" ]; then
    xrc advise/v0
    advise rm proxy
    proxy_log "advise proxy time: "
    time advise proxy
fi